---
---
// src/layouts/Layout.astro
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Remnants of Destruction</title>
    <link rel="stylesheet" href="/rodstyles.css" />
    <!-- You can include additional global styles here if needed -->
    <style>
        /* Global Styles */
        body, html { margin: 0; padding: 0; overflow: hidden; }
        #gameCanvas { display: block; }
        /* Tooltip Styles */
        .tooltip {
            position: absolute;
            background-color: rgba(0, 0, 0, 0.8);
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            pointer-events: none;
            font-size: 12px;
            z-index: 1000; /* Ensure it's above other popups like skillTree */
            display: none;
            max-width: 200px;
        }
        /* Additional global styles can be added here */
    </style>
</head>
<body>
    <slot />
    <!-- Include any global scripts here if necessary -->
</body>
</html>
